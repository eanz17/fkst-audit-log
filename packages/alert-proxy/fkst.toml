kind = "package"
name = "alert-proxy"
persistence_class = "stateless_adapter"

[code]
root = "."

[lib_deps]
libraries = ["audit_shared"]

[dependency_constraints]
allowed_lib_deps = ["audit_shared"]
